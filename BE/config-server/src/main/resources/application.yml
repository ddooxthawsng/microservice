server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # Lấy config trực tiếp từ folder config-repo trong src/main/resources
          search-locations: classpath:/config-repo/
